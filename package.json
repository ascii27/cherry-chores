{
  "name": "cherry-chores",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w web && npm run build -w api",
    "start:prod": "node api/dist/index.js",
    "test": "npm run test -w api && npm run test -w web",
    "lint": "echo 'Add linters in Phase 0/1'",
    "format": "echo 'Add prettier in Phase 0/1'",
    "ci": "npm run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
